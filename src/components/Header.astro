---
import ThemeToggle from './ThemeToggle.astro';

const links = [
  { href: '/', label: 'Start' },
  { href: '/#services', label: 'Leistungen' },
  { href: '/#skills', label: 'Kompetenzen' },
  { href: '/projekte/', label: 'Projekte' },
  { href: '/ki-agenten-lab/', label: 'KI-Lab' },
  { href: '/ueber-mich/', label: 'Über mich' },
  { href: '/kontakt/', label: 'Kontakt' }
];
---
<header class="sticky top-0 z-50 border-b border-white/10 bg-gray-950/80 backdrop-blur dark:border-white/10 dark:bg-gray-950/80 light:border-gray-200 light:bg-white/80">
  <div class="container mx-auto flex items-center justify-between px-4 py-4">
    <a href="/" class="text-sm font-semibold uppercase tracking-[0.4em] text-blue-400 dark:text-blue-400 light:text-blue-600">alexle135</a>
    <nav aria-label="Hauptnavigation">
      <ul class="flex items-center gap-4 text-sm font-medium text-gray-300 dark:text-gray-300 light:text-gray-700">
        {links.map((item) => (
          <li>
            <a
              href={item.href}
              class="transition hover:text-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-400 dark:hover:text-white light:hover:text-gray-900"
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    <div class="flex items-center gap-3">
      <ThemeToggle />
      <a
        href="mailto:schneider@alexle135.de"
        class="hidden rounded bg-blue-600 px-4 py-2 text-sm font-medium text-white transition hover:bg-blue-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-400 md:inline-flex dark:bg-blue-600 dark:hover:bg-blue-500 light:bg-blue-600 light:hover:bg-blue-700"
      >
        Zusammenarbeit starten
      </a>
    </div>
  </div>
</header>
